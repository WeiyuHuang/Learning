README

Learning Correlation Analysis Project version 1

----------------------------------------------------------
DATA FOLDER

Put learning_v1_Data folder in the same root directory as learning_v1 folder so the scripts can run directly

6_week_learning/all_home_scanner_weighted.mat20X6 (subjXseq) cell arraycol1 = cumulative trialcol2 = session (99 = scan training, else = home training)col3 = home training group (1 = between scan1,2; 2 = between scan2,3; 3 = between scan3,4);col4 = MTcols5-9 weights (not really that essential)
----------------------------------------------------------
INTERMEDIATE_RESULTS FOLDER

Depository to put all intermediate results that will be loaded when scripts calling these results already computed.